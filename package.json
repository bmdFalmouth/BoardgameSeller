{
  "name": "gaboardgameclub",
  "version": "1.0.0",
  "description": "This will collect the boardgame collection of the GA and perhaps as a way to organise sessions",
  "author": "Brian McDonald",
  "private": true,
  "scripts": {
    "dev": "cross-env NODE_ENV=development nodemon server/index.js --watch server --watch api",
    "build": "nuxt build",
    "start": "cross-env NODE_ENV=production node server/index.js",
    "generate": "nuxt generate"
  },
  "dependencies": {
    "axios": "^0.19.0",
    "bootstrap": "^4.1.3",
    "bootstrap-vue": "^2.0.0-rc.11",
    "cross-env": "^5.2.0",
    "express": "^4.18.2",
    "nuxt": "^2.0.0",
    "xml-js": "^1.6.11"
  },
  "devDependencies": {
    "nodemon": "^1.19.1"
  }
}
